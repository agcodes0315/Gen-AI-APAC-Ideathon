Replace only:
src/styles/mirrortrace-final-visual-fix.css

Do not change any TSX file.

After replacing:
1. npm run lint
2. npm run build
3. npm run dev
4. Hard refresh Chrome with Ctrl + Shift + R

The Security & Operations outer hero is forced to rgba(0,0,0,0.30).
The Privacy Boundary remains lighter inner glass.
