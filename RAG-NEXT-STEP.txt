MirrorTrace RAG upgrade — files needed for a real implementation

Please provide the CURRENT versions of:
1. server.ts
2. server/gemini.ts
3. server/firestoreService.ts
4. src/lib/api.ts
5. src/types.ts
6. firestore.rules

Why:
- RAG must be implemented server-side.
- Retrieval must always be scoped to the verified Firebase UID.
- Only approved memories should be eligible as reusable retrieval context.
- The Gemini prompt should receive source IDs so the UI can show provenance.
- We should not claim vector / semantic RAG in the README until this backend path exists.

Recommended 3-day-safe version:
- retrieve the signed-in user's approved Thought Snapshots
- rank candidate memories by deterministic topic/tag overlap first
- send only top-k relevant memories to Gemini
- require structured output containing usedSnapshotIds
- validate every returned source ID belongs to the authenticated owner
- show the used sources in the provenance UI

Optional later:
- embeddings / vector search
- hybrid semantic + keyword retrieval
- evaluation metrics for retrieval precision and faithfulness
