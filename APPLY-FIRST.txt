MIRRORTRACE ACTUAL SHARP FIX

ROOT CAUSE FOUND:
Your code was requesting:
  /hero/mirrortrace-poster.jpg

But the verified sharp tree photo is:
  mirrortrace-poster.jpeg

That filename/extension mismatch meant the intended sharp image was not the
asset being rendered, allowing the old/fallback hero imagery to remain visible.

REPLACE THESE FILES:
  src/components/DashboardOverview.tsx
  src/components/AdminDashboard.tsx
  src/index.css

COPY THIS EXACT IMAGE:
  public/hero/mirrortrace-poster.jpeg

IMPORTANT:
Delete or rename any old public/hero/mirrortrace-poster.jpg so there is no confusion.

THEN RUN:
  npm run lint
  npm run build
  npm run dev

OPEN THIS DIRECTLY IN CHROME TO VERIFY THE ASSET FIRST:
  http://localhost:3000/hero/mirrortrace-poster.jpeg

You MUST see the sharp branches photo by itself.
Then hard refresh the app:
  Ctrl + Shift + R

The 403 /api/admin/overview message is unrelated to the hero blur. It means
the currently signed-in Firebase token does not have an admin role.
