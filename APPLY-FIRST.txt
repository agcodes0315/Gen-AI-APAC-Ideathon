MIRRORTRACE UI RECOVERY PACKAGE

Why Vite failed:
AdminDashboard.tsx imports ../styles/mirrortrace-clean-glass.css and
../styles/mirrortrace-authenticated-haze.css, but those files were missing.
AuthView.tsx also imports several related style files.

This package restores every referenced stylesheet and replaces the old stack
of conflicting visual patches with ONE visual authority:

  src/styles/mirrortrace-final-visual-fix.css

Do this:
1. Copy the entire src/ folder from this package over your project's src/ folder.
2. Allow Windows/VS Code to replace files with the same names.
3. Do NOT edit AuthView.tsx, AdminDashboard.tsx, DashboardOverview.tsx,
   App.tsx, Navbar.tsx, index.css, your videos, or your images for this fix.
4. Stop the dev server (Ctrl+C).
5. Run:
      npm run lint
      npm run build
      npm run dev
6. Hard refresh Chrome with Ctrl+Shift+R.

Expected result:
- Landing hero video remains intact.
- /hero/mirrortrace-poster.jpeg remains the shared branch background.
- Your Thinking, Versioned = transparent bg-black/30 over branches.
- Security & Operations = transparent bg-black/30 over branches.
- KPI cards, Perspective Evolution, Quick Actions, admin metrics,
  Service Health, Reflection Infrastructure, Users, Support, Reviews,
  Audit, feature cards, security cards and public-review cards all use
  the same subtle bg-black/30 surface.
- Vertical scrolling remains native and easy.
- Feature rows still browse horizontally with smooth scroll.
- No old 18px/20px backdrop-filter stacks remain in these replacement files.

IMPORTANT:
The branch image must exist at:
  public/hero/mirrortrace-poster.jpeg

Your screenshot already showed that file in public/hero, so no asset rename is
required.
