MIRRORTRACE — ROOT FILE CLEANUP
===============================

DEFINITELY REMOVE
-----------------
1. src/mirrortrace-motion.css
   Remove it if its only content is:
      @import './styles/mirrortrace-final-visual-fix.css';
   That target file is deleted, so this shim can only create another 404.
   The included cleanup script handles this automatically.

SAFE NON-RUNTIME CLUTTER YOU CAN REMOVE IF YOU NO LONGER NEED THE NOTES/BACKUPS
-------------------------------------------------------------------------------
From the screenshots, these are not normal Vite runtime files and can be moved
out of the repository or deleted once you have confirmed you do not need them:

  Pasted code.css
  APPLY-FIRST.txt
  APPLY.txt
  GITHUB-PUSH.txt
  PRODUCTION-CHECKLIST*.txt
  RAG-NEXT-STEP.txt
  README_INSTALL.txt
  README-APPLY.txt
  README-FIX.txt
  README-HARDENING.txt
  README-INTEGRATION.txt
  README.txt
  patch-email-routes.ps1
  patch-notifications.ps1
  untitled.tsx
  untitled-1.tsx

LIKELY DUPLICATES — REMOVE ONLY AFTER CONFIRMING NOTHING IMPORTS THEM
--------------------------------------------------------------------
  DashboardOverview.tsx        (root copy; real component is under src/components)
  index.css                    (root copy; real application stylesheet appears under src)
  mirrortrace_reflection_car*.css
  mirrortrace_secondary_p*.css
  mirrortrace_updated/         (looks like a backup/export folder)

GENERATED — SAFE TO REBUILD
---------------------------
  dist/

DO NOT REMOVE
-------------
  src/
  public/
  server/
  scripts/                     (unless you intentionally retire the scripts)
  package.json
  package-lock.json
  tsconfig.json
  vite.config.ts
  firestore.rules
  firebase-*.json
  .env / .env.example
  README.md
  node_modules/                (optional to delete, but npm install would be required again)

One-command check before deleting any likely duplicate:
  Get-ChildItem . -Recurse -File | Select-String -Pattern 'DashboardOverview.tsx|mirrortrace_reflection_car|mirrortrace_secondary_p' | Select Path,LineNumber,Line
