REPLACE COMPLETELY:
src/components/DashboardOverview.tsx
src/components/AdminDashboard.tsx
src/index.css

REQUIRED EXISTING IMAGE:
public/hero/mirrortrace-poster.jpg

This uses the SAME sharp image as the landing page.
There is no blur on the image.
The readability overlay is only 4%–20% dark depending on position.

Then run:
npm run lint
npm run build
npm run dev

Hard refresh Chrome:
Ctrl + Shift + R
